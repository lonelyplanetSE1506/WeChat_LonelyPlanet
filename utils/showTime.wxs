var showTime = {
  timetoStr:function(date){
    //console.log(date)
    return date.substring(0, 4) + '年' + date.substring(5, 7) + '月' + date.substring(8, 10) + '日' + ' ' + date.substring(11, 16)
  }
}
  module.exports={
    timetoStr:showTime.timetoStr
  }